spring:
  application:
    name: dubbo-provider

dubbo:
  registry:
    address: zookeeper://127.0.0.1:2181
    protocol: zookeeper
  protocol:
    name: dubbo
    port: 20880
#  scan:
#    base-packages: com.meng.mydubbo.provider.service.impl

logging:
  level:
    root: debug
server:
  port: 8081
